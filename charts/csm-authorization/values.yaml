authorization:
  proxyServiceImage: dellemc/csm-authorization-proxy:nightly
  tenantServiceImage: dellemc/dellemc/csm-authorization-tenant:nightly

  hostname: csm-authorization.com

  logLevel: debug

  zipkin: {}
    #collectoruri: http://DNS-hostname:9411/api/v2/spans
    #probability: 1

redis:
  storageClass: local-storage
